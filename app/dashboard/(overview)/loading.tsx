// No se por que pasa si importo el card con las llaves funciona si no toma el por defecto
import  DashboardSkeleton  from '@/app/ui/skeletons';
 
export default function Loading() {
  return <DashboardSkeleton />;
}